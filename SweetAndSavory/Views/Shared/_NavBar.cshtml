<nav class="navbar navbar-expand-lg navbar-dark bg-dark rounded">
  <a class="navbar-brand" href="#">Pierre's</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample09" aria-controls="navbarsExample09" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarsExample09">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link home" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/Treats">Treats</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/Flavors">Flavors</a>
      </li>
      @if(!User.Identity.IsAuthenticated)
      {
        <li class="nav-item">
          <a class="nav-link" href="/Account/Register">Register</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/Account/Login">Log in</a>
        </li>
      }
    </ul>
    @if(User.Identity.IsAuthenticated)//user is signed in
    {
      <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            @* <a href= "#"class="nav-link content">Hello @User.Identity.Name</a> *@
          </li>
          <li class="nav-item">
            @using (Html.BeginForm("LogOff", "Account"))
            {
              <input class= "btn btn-dark content nav-link" type="submit" value= "Log Out: @User.Identity.Name">
            }
          </li>
      </ul>
    }
  </div>
</nav>
@* <nav class="navbar navbar-light bg-light">
  <form class="form-inline ml-auto">
      @using (Html.BeginForm("search","Categories")) 
          {
            @Html.TextBox("Search", null, new {@class="form-control mr-sm-2"})
            <input type="submit" value="Search" class="btn btn-outline-dark my-2 my-sm-0">
          }
  </form>
</nav> *@